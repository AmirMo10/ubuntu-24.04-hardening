# Temporary repository bootstrap

This temporary file is used only to transfer the prepared Git history into the newly created empty repository. It will be removed automatically when the bootstrap workflow replaces this branch with the validated release commit.
